{
  "name": "restaurant-app",
  "private": true,
  "scripts": {
    "dev": "dotenv -- turbo run dev",
    "build": "turbo run build",
    "start": "dotenv node ./apps/api/dist/main.js"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "turbo": "^1.9.3"
  }
}
